/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../../out-tsc/app",
    "types": [],
    // Fase 3 (Native Federation): {} sobrescreve os paths svg-engine->dist do raiz
    // para resolver via node_modules (file:dist/svg-engine) e o federation
    // compartilhar o engine como PACOTE (import map correto).
    "paths": {}
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.spec.ts"]
}
